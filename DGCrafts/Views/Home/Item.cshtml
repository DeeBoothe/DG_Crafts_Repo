@model DGCrafts.Models.Item
@{
    ViewData["Title"] = "Item to Buy";

    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}


    <div class="text-center">

        @await Component.InvokeAsync("ImageRetriever", new { itemId = Model.ItemID })

        @*<div id="itemCarousel" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">

                </div>
                <div class="carousel-item">
                    <img src="~/Images/default-image.jpg" />
                </div>
                <div class="carousel-item">
                    <img src="~/Images/default-image.jpg" />
                </div>
            </div>
            <a class="carousel-control-prev" href="#itemCarousel" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            </a>
            <a class="carousel-control-next" href="#itemCarousel" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
            </a>*@
        </div>


        <div class="text-left">
            <h4>@Model.ItemName</h4>

            <div>
                <b>@Model.UnitPrice.ToString("c")</b>
            </div>

            <div>
                <p>@Model.ItemDescription</p>
            </div>

            <div>
                <form asp-controller="Cart" asp-action="AddToCart">
                    <select  name="quantity">
                        @for (int i = 1; i <= Model.AmtAvailable; i++)
                        {
                            <option value="@i">@i</option>
                        }
                    </select>
                    <div class="text-right">
                        <input type="hidden" asp-for="@Model.ItemID" />
                        <input class="btn btn-dark" value="Add to Cart" type="submit" />


                    </div>
                </form>



            </div>

        </div>


    </div>
 

       